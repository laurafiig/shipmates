# ShipMates

An app to help current and former navy personnel keep in touch with those they have served with

Created using React

actions:

dependencies

list of child components
	create main
	create children
	create routes

list of schema
	create schema

reseach using sql
	add to dependencies
	create server